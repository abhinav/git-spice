# Architecture decision records

The `design/` directory is the git-spice architecture decision log.
Use `YYYY-MM-DD-slug.md` to name the files.

New records should identify one decision
and its status, context, and material consequences.

Do not rewrite an accepted decision when the direction changes.
Create a new record that supersedes it,
then link the old and new records in both directions.
